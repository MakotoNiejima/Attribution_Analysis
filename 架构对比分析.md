# 归因分析 vs 舆情分析 vs 电商客服 — 架构对比分析

## 三项目架构对比总览

| 维度 | 归因分析 (新) | 舆情分析 (教) | 电商客服 (教) |
|------|------------|------------|------------|
| **定位** | 单用户分析工具 | 多智能体研究报告系统 | 多轮对话客服系统 |
| **Agent 框架** | LangGraph 单图 | LangGraph × 3 + 事件总线 | 自研 DialogueEngine |
| **Agent 拓扑** | 线性流水线 + 条件分支 | 并发双 Agent + 事件驱动 Judge | 三轨路由 (task/knowledge/chitchat) |
| **状态模型** | dataclass 可变状态 | TypedDict (per-agent) | DDD Aggregate Root |
| **DI 方式** | `@lru_cache` 单例 | 模块级单例 + getter | FastAPI `Depends()` |
| **持久化** | SQLAlchemy 同步 + run_in_threadpool | 文件 (.md) 读写 | SQLAlchemy async |
| **实时推送** | WebSocket (内存事件) | SSE (事件总线→广播) | 无 |
| **RAG** | FAISS/NumPy + SQLite | Milvus 双向量 + BgeM3 | Provider 模式 (stub) |
| **LLM 调用** | 节点内直接调用，无重试 | 带重试+退避+优雅降级 | 无重试，异常直接上抛 |
| **错误处理** | 图级 error 节点 + API 层 catch | 重试装饰器 + 事件级隔离 | 验证器拦截 → 澄清 |
| **测试** | 有 test 文件 | 有 test 文件 | 无 |

---

## 一、分层与关注点分离

### 归因分析 ⭐⭐⭐⭐

```
API → Service → Agent/Graph → AnalysisCore
                                ↘ RAG
                                ↘ Persistence
                                ↘ Attachments
```

- 层次清晰：`analysis_core/` 纯数学计算，`agent/` 纯流程编排，`persistence/` 独立存储
- **不足**：`analysis_core/service.py` 里混用了 `print()` 和 `progress_callback`；两个独立的 SQLAlchemy Engine 管理不够显式

### 舆情分析 ⭐⭐⭐⭐⭐

```
app/ (API + Services) → engines/ (自包含)
  engines/common/     → 共享基础设施
  engines/contracts/  → 接口契约
  engines/host_agent/ → 判断智能体
  engines/insight_agent/ → 私域检索
  engines/media_agent/   → 公域检索
  engines/report_engine/ → 报告合成
```

- **最佳实践**：`engines/` 完全自包含，不依赖 `app/`；`contracts/` 定义接口边界
- 共享节点 (`FormatReportNode`, `SaveReportNode`) 避免了重复
- 事件总线解耦了 Agent 间的通信

### 电商客服 ⭐⭐⭐⭐⭐

```
API → Service → Engine → Handler → Provider/Action
                ↗ Domain (纯领域模型)
                ↗ Plan (LLM规划)
                ↗ Task (命令+流程+动作)
                ↗ Knowledge (知识提供者)
```

- **DDD 最佳实践**：`domain/` 零框架依赖，`to_dict()`/`from_dict()` 手动序列化
- 层次最丰富：API → Service → Engine → Handler → Provider/Action 五层架构
- `infrastructure/` 独立管理外部依赖（LLM、DB、HTTP）

**结论**：电商客服的分层最讲究，领域模型最纯净；舆情分析的模块边界最清晰（engines 自包含）；归因分析分层合理但略显扁平。

---

## 二、可扩展性设计

### 归因分析 ⭐⭐⭐

- 扩展方式：在 `nodes/` 下新增文件 → 在 `graph.py` 中添加节点和边
- **扩展需要改图定义**，没有插件机制
- 分析核心 (`analysis_core/`) 是硬编码的业务逻辑

### 舆情分析 ⭐⭐⭐⭐

- Agent 层：新增 Agent 需改 `_RESEARCH_INVOKER`、`ROLE_INFOS` 等多处，**注册机制缺失**
- 搜索 Provider：`_PROVIDER_ADAPTERS` 字典模式，可扩展
- DB 搜索：`PLATFORM_MAPPING` 字典模式，新增平台需加 SQL 模板

### 电商客服 ⭐⭐⭐⭐⭐

- **Action 自动发现**：`importlib`/`pkgutil` 扫描 `task/action/customer/` 目录，新增 Action 零配置
- **Knowledge Provider**：实现 `KnowledgeProvider` 接口 + 注册即生效
- **Flow 系统**：YAML 声明式定义，修改业务流程不改代码
- **Command 模式**：新增命令类型只需加类 + 注册到 `COMMAND_TO_CLASS`

**结论**：电商客服的扩展性最强，三个维度（Action/Provider/Flow）都有自动发现或声明式配置；舆情分析靠字典注册，需要手动改多处；归因分析扩展需要改图定义。

---

## 三、状态管理与对话能力

### 归因分析 ⭐⭐⭐

- 单个 `AnalysisState` dataclass 流经所有节点
- 支持多轮对话（`conversation_history` + `context_summaries`）
- 会话压缩避免上下文溢出
- **不足**：状态是进程内的，无分布式状态管理

### 舆情分析 ⭐⭐⭐⭐

- 每个 Agent 独立的 `TypedDict` State
- Host Agent 的 `Session` 模式巧妙地桥接了多次图调用的状态
- cursor-based 循环处理多 section
- **不足**：无持久化，进程重启丢失所有状态

### 电商客服 ⭐⭐⭐⭐⭐

- **DDD Aggregate Root**：`DialogueState` 是完整的领域聚合
- **中断/恢复栈**：`interrupted_active_tasks` 支持任务切换
- **Session 生命周期**：60 分钟超时自动关闭
- **全量 JSON 序列化**：每轮存 MySQL，支持恢复
- **不足**：全量序列化随对话增长会膨胀；无乐观锁，并发写入有丢失风险

**结论**：电商客服的状态管理最成熟，DDD 聚合 + 中断栈 + 持久化是生产级设计；舆情分析的 Session 模式巧妙但无持久化；归因分析功能完整但设计较简单。

---

## 四、容错与健壮性

### 归因分析 ⭐⭐⭐

- 图级 error 节点 + 条件路由
- 进程重启时 `fail_interrupted_tasks()` 恢复
- 任务取消支持 `cancel_requested` 标志
- **缺失**：LLM 调用无重试，无断路器

### 舆情分析 ⭐⭐⭐⭐⭐

- **重试机制最完善**：`with_retry` + `with_graceful_retry`，指数退避，非可重试错误分类
- 事件总线隔离：一个 subscriber 失败不影响其他
- Host Agent 优雅降级：`@with_graceful_retry` 返回 None
- DB 搜索错误被捕获到 Response 对象而非上抛
- **不足**：无任务持久化，进程重启丢失进行中的研究

### 电商客服 ⭐⭐

- `TurnPlanValidator` 拦截 LLM 错误输出 → 路由到澄清
- **缺失**：LLM 调用无 try/except；Action 失败直接上抛；无重试；外部 API 无超时/断路器

**结论**：舆情分析的容错设计最专业（重试+退避+优雅降级+事件隔离）；归因分析有图级容错但缺重试；电商客服的容错最薄弱。

---

## 五、实时能力

### 归因分析 ⭐⭐⭐⭐⭐

- WebSocket 双向通信
- `TaskRuntimeManager` 实现内存事件广播
- per-node 进度推送 (`node_started` / `node_completed`)
- WebSocket Token 短期能力令牌

### 舆情分析 ⭐⭐⭐⭐

- SSE 单向推送
- 事件总线 → `BroadcasterService` → SSE 客户端队列
- 支持多 Agent 并发进度
- **不足**：无背压控制，队列可能无限增长

### 电商客服 ⭐

- 无实时推送机制
- 同步请求-响应模式

**结论**：归因分析的 WebSocket 最完整；舆情分析的 SSE 方案够用；电商客服无实时能力。

---

## 六、安全性

### 归因分析 ⭐⭐⭐

- HMAC-SHA256 Token 认证
- 管理员角色分离
- **不足**：LLM 提示无注入防护；配置模块级可变全局变量有竞态风险

### 舆情分析 ⭐⭐

- **无任何认证**：CORS 允许所有来源
- 无输入验证

### 电商客服 ⭐⭐

- 无认证（教学项目）
- **`eval()` 安全隐患**：流程条件用 `eval()` 执行，虽限制了 globals，但 locals 含真实数据

**结论**：归因分析有基本安全机制；舆情分析和电商客服作为教学项目安全设计缺失；电商客服的 `eval()` 是最严重的安全隐患。

---

## 七、总结评分

| 维度 | 归因分析 | 舆情分析 | 电商客服 |
|------|---------|---------|---------|
| 分层设计 | ⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ |
| 可扩展性 | ⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ |
| 状态管理 | ⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ |
| 容错健壮 | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐⭐ |
| 实时能力 | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐ |
| 安全性 | ⭐⭐⭐ | ⭐⭐ | ⭐⭐ |
| 代码组织 | ⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ |
| **综合** | **⭐⭐⭐½** | **⭐⭐⭐⭐** | **⭐⭐⭐½** |

---

## 八、归因分析可以向教学项目借鉴什么

### 从舆情分析借鉴：

1. **重试机制**：引入带退避的 LLM 调用重试 + 优雅降级，这对单图流水线尤其重要——任何节点 LLM 失败整个任务就挂了
2. **BaseNode 抽象**：当前每个节点是独立函数，可以提取公共的 progress/logging/retry 逻辑到基类
3. **事件总线**：当前 `TaskRuntimeManager` 可以参考舆情分析的 typed event + subscriber 隔离设计

### 从电商客服借鉴：

1. **DI 容器**：`@lru_cache` 单例不利于测试，应改用 FastAPI `Depends()` 注入 persistence service
2. **知识 Provider 模式**：RAG 和附件查询可以抽象为 `KnowledgeProvider`，便于后续扩展新数据源
3. **Action 自动发现**：如果未来要支持自定义分析动作，可以参考 `importlib` 扫描注册
4. **流程 YAML 声明化**：当前图是硬编码的，如果分析流程有变种，可以考虑声明式配置

### 归因分析已有的优势（教学项目没做到的）：

1. **任务持久化 + 崩溃恢复** — 舆情分析没有
2. **WebSocket 实时推送** — 电商客服没有
3. **证据验证门控** (`validate_report`) — 其他两个都没有
4. **多轮对话上下文压缩** — 舆情分析没有
5. **热配置重载** — 其他两个都没有
